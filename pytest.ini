[pytest]
# Collect from tests/ only. lightrag_setup/ contains files named test_* that
# are runnable scripts against a live vector store, not unit tests.
testpaths = tests

# Without this, plain `pytest` cannot resolve the framework.* imports; only
# `python -m pytest` works, because that puts the cwd on sys.path.
pythonpath = .
